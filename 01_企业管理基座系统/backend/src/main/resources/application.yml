server:
  port: ${PORT:9001}

spring:
  datasource:
    url: jdbc:postgresql://${DB_HOST:dpg-d8n7tncm0tmc73dr44n0-a.oregon-postgres.render.com}:${DB_PORT:5432}/${DB_NAME:enterprise_base}
    username: ${DB_USERNAME:agent_user}
    password: ${DB_PASSWORD:}
    driver-class-name: ${DB_DRIVER:org.postgresql.Driver}

mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  global-config:
    db-config:
      id-type: auto
      logic-delete-field: deleted
      logic-delete-value: 1
      logic-not-delete-value: 0

jwt:
  secret: ${JWT_SECRET:Y2hhbmdlLXRoaXMtdG8tYS1sb25nLXNlY3JldC1rZXktMTIzNDU2Nzg5MA==}
  expiration: 86400000

ai:
  api-key: ${AI_API_KEY:sk-your-api-key-here}
  api-url: ${AI_API_URL:https://api.deepseek.com/v1/chat/completions}
  model: ${AI_MODEL:deepseek-chat}
